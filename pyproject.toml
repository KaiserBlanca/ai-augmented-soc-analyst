[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-augmented-soc-analyst"
version = "0.1.0"
description = "AI-Augmented SOC Analyst CLI Tool"
authors = [{ name = "Emre Akhanlı" }]
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
